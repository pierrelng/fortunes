{% extends "base.html.twig" %}

{% block title %}Fortunes{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/styles.css') }}" />
{% endblock %}

{% block body %}
<div class="container">
    <div class="row mainBody">

        <div class="container-cards col-md-3"></div>

        <div class="container-cards col-md-6">
            {% for item in entities %}
                {{ include('default/fortune.html.twig') }}
            {% endfor %}
            <div class="containerPagination">{{ pagerfanta(entities, 'twitter_bootstrap3') }}</div>
        </div>

        <div class="container-cards col-md-3"></div>

    </div>
</div>
{% endblock %}